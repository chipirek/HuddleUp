<%= form_for [@project, @issue, @post], :html => {:class => 'form-horizontal'} do |f| %>

          <textarea id="post_body" name='post[body]' autofocus="true" class="autosize-transition span12" style='font-size:2em; overflow: hidden; word-wrap: break-word; resize: horizontal; height: 100px;'><%= @post.body %></textarea>

    <div style='display:none;'>
      <button type='submit' class='btn btn-success'><i class="icon-ok"></i> Save</button>
    </div>

<% end %>
